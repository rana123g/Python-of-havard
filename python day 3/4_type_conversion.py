# We can convert one type of data into another type of data using various functions
a = input(("What's a: ")) # Notice here " " this is used so the data is string form
b = input(("What's b: "))# Notice here " " this is used so the data is in string form
 # the int would conver the data into integer form and then we can add them together and store it in c
c = int(a) + int(b)
print(c)